/// <referece types="cypress" />

describe('Work whit alert', () => {
    before(
        () => {
        cy.visit('https://wcaquino.me/cypress/componentes.html');
    });

    beforeEach(
        () => {
            cy.reload();
        }
    );

    //iniciar o desafio aula 35

})